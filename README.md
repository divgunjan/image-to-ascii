pillow library used for converting images into ascii-art.

### directions: 
1. open pillolib.py as a writeable file in an ide,
2. in line 6, insert the name of the image file with the correct file and path name ```img = Image.open("")```
3. open the newly created .txt file